{% extends 'base.html.twig' %}

{% block title %}Liste des stagiaires{% endblock %}

{% block body %}

<div class="wrapper">
    <h1>Liste des stagiaires</h1>
    <div class="listStagiaire">
    {% for stagiaire in stagiaires %}
        <a href="{{ path('show_stagiaire', {'id': stagiaire.id}) }}">{{ stagiaire }}</a> <a href="{{ path('edit_stagiaire', {'id':stagiaire.id}) }}">Editer</a><br>        
    {% endfor %}
    </div>

    {# <a href="{{ path('delete_formation', {'id':formation.id}) }}">Supprimer</a>  #}
    <a href=" {{ path('add_stagiaire') }}">Ajouter stagiaire</a>
</div>
{% endblock %}
